using System.Collections;
using System.Collections.Generic;
using GD;
using UnityEngine;

[CreateAssetMenu(fileName = "InteractionEvent", menuName = "ScriptableObjects/Events/InteractionEvent", order = 0)]
public class InteractionEvent : BaseGameEvent<InteractableData>
{
    // Start is called before the first frame update
}
